import { UrlEncodePipePipe } from './url-encode-pipe.pipe';

describe('UrlEncodePipePipe', () => {
  it('create an instance', () => {
    const pipe = new UrlEncodePipePipe();
    expect(pipe).toBeTruthy();
  });
});
